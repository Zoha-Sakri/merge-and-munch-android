# Android Codex Context

Implement original farming-city gameplay in Kotlin/Compose. State belongs in `GameViewModel`; UI belongs in `MainActivity` composables. The intended loop is farm -> merge -> trade -> town upgrades -> tunnel exploration.

Build with Android Studio on API 29+ and report native build results accurately.